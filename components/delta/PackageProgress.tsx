export function PackageProgress({ complete, total }: { complete: number; total: number }) { const percentage = total === 0 ? 100 : Math.round((complete / total) * 100); const widths: Record<number, string> = { 0: "w-0", 25: "w-1/4", 50: "w-1/2", 75: "w-3/4", 100: "w-full" }; return <div><div className="flex justify-between text-[11px] text-text-tertiary"><span>Review progress</span><span>{complete}/{total} reviewed</span></div><div className="mt-2 h-1 overflow-hidden rounded-full bg-surface-tertiary"><div className={`h-full rounded-full bg-accent-primary transition-all duration-300 ${widths[percentage] ?? "w-full"}`} /></div></div>; }
